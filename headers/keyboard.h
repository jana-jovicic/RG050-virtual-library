#ifndef _KEYBOARD_H_
#define _KEYBOARD_H_

#include "../headers/books.h"

void keyboardFunc(unsigned char, GLint, GLint);
void specialFunc(GLint, GLint, GLint);
void createNewWindow(Book book);

#endif